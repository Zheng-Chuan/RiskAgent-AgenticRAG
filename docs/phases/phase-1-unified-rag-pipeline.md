# Phase 1 Unified RAG Pipeline

## 目标

把默认执行链路收敛成统一高可信 RAG 主链.

## 本阶段交付

- 固定主链 `rewrite -> retrieve_and_critique -> revise_query loop -> synthesize_answer -> validate_and_save`
- 固定默认检索链 `hybrid + query intelligence + advanced index`
- 输出结构化证据链

## 验收标准

- 主路径可以稳定运行
- 文档和代码对主链描述一致
- API 和 CLI 都复用同一条主链

## 状态

Completed
